name: CI - Build, Test, and Lint

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    name: Build, Test, and Lint

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: Check for Node project
        id: check-node
        run: |
          if [ -f package.json ]; then echo "has_node=true" >> $GITHUB_ENV; else echo "has_node=false" >> $GITHUB_ENV; fi

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: env.has_node == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        if: env.has_node == 'true'
        run: npm ci

      - name: Build project
        if: env.has_node == 'true'
        run: npm run build

      - name: Run linter
        if: env.has_node == 'true'
        run: npm run lint

      - name: Run tests
        if: env.has_node == 'true'
        run: npm run test

      - name: Upload coverage
        if: env.has_node == 'true'
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/coverage-final.json
          flags: unittests
          name: codecov-umbrella

  security-check:
    runs-on: ubuntu-latest
    name: Security Audit

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run security audit
        run: npm audit --audit-level=moderate || true

  pr-validation:
    runs-on: ubuntu-latest
    name: PR Validation
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate PR title
        uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          validateSingleCommit: false
          validateSingleCommitMatchesPrTitle: false

      - name: Check commit messages
        run: |
          echo "Commit message validation would go here"
          echo "This is a placeholder for commit linting"
